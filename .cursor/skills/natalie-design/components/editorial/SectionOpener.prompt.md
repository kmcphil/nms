One-line: opens every major section on the site; numbers run in sequence across the page.

```jsx
<SectionOpener number="03" title="The Method" dateline="The framework" />
```

The number is one of the only red elements permitted.
