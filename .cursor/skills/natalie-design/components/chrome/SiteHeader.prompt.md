One-line: page chrome at the top of every screen.

```jsx
<SiteHeader activeHref="/about" />
```

Logo height is 44px (footer 40px) and must never be retyped. Nav labels are uppercase with 0.16em tracking.
