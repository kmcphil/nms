One-line: sits directly under the hero as a quiet proof line.

```jsx
<BragBar logos={[{name:'Tory Burch Foundation', src:'/assets/logos/tory-burch.svg'}]} />
```

Marks are greyscaled to 70% opacity — never full-colour client logos.
