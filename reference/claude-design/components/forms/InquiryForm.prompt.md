One-line: the Start the Conversation form — qualifies rather than books.

```jsx
<InquiryForm status="idle" onSubmit={handle} />
```

The note under the fields is part of the design: no automatic scheduling links.
