/** Hand-drawn underscore under a single key word. Use at least twice per page, never twice in one headline. */
export interface HandUnderscoreProps {
  children?: React.ReactNode;
  /** cream on red/ink backgrounds */
  tone?: 'ink' | 'cream';
}
export function HandUnderscore(props: HandUnderscoreProps): JSX.Element;
