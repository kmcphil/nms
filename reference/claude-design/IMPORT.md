# Claude Design System import

**Source:** `C:\Users\kurst\Downloads\Natalie Micale Schultz Design System.zip`

## Where it landed

| Location | Purpose |
|----------|---------|
| `.cursor/skills/natalie-design/` | Cursor agent skill (SKILL.md, tokens, components, guidelines, UI kit, assets) |
| `reference/claude-design/` | Same screens/tokens/assets for humans + PRs |

**Skipped from the zip:** `uploads/` (PDF + logo-pack duplicates already under `reference/` / `public/brand/`), `_ds_bundle.js`, oxlint adherence file.

## Sync note

Claude Design was built **from** `github.com/kmcphil/nms` (`main`). Prefer production `src/` when Cursor and the kit disagree on hero polish already approved in this repo.
